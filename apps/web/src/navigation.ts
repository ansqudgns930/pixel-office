export function pixelOfficeReturnUrl(search:URLSearchParams,runId:string,stored:{companyId?:string|null;projectId?:string|null;agentId?:string|null}={}):string{const target=new URLSearchParams(),companyId=search.get("companyId")??stored.companyId??"demo-company",projectId=search.get("projectId")??stored.projectId,agentId=search.get("agentId")??stored.agentId;target.set("companyId",companyId);if(projectId)target.set("projectId",projectId);if(agentId)target.set("agentId",agentId);if(runId)target.set("runId",runId);return `/pixel-office?${target.toString()}`;}
