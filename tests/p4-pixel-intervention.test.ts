import test from "node:test";import assert from "node:assert/strict";import {SQLiteStateStore} from "../packages/persistence/src/index.js";import {ProjectOperations} from "../packages/project-ops/src/index.js";import {CompanyOperations} from "../packages/company-ops/src/index.js";import {OperationalStore} from "../packages/operations/src/index.js";import {OfficeProjectionStore} from "../packages/office-projection/src/index.js";import {RunController} from "../packages/runtime/src/index.js";import {InterventionService} from "../packages/intervention/src/index.js";
class Queue{async enqueue(){}async remove(){return false}}
test("P4 intervention event and resulting Run state reach Pixel Office projection",async()=>{const state=new SQLiteStateStore(":memory:"),projects=new ProjectOperations(state.db,state),companies=new CompanyOperations(state.db,state,projects),operations=new OperationalStore(state.db),controller=new RunController(state,new Queue()),interventions=new InterventionService(state.db,state,controller,projects);projects.createWorkspace("w","W");projects.createProject({id:"p",workspaceId:"w",name:"P",repoPath:".",defaultBranch:"main",runtimePath:".",organizationProfile:{},budgetLimit:5},"owner");projects.createTask({id:"t",projectId:"p",milestoneId:null,title:"T",status:"ready",priority:1,completionCriteria:["done"],budgetLimit:5},{id:"owner"});companies.createCompany({id:"c",name:"C",workspaceId:"w",budgetLimit:5,mandatoryReviews:["review"],mandatoryApprovals:["result"],allowedTools:["test"]},"owner");companies.createDepartment({id:"d",companyId:"c",parentId:null,name:"D",budgetLimit:5},"owner");companies.linkProject("c","d","p",1,"owner");await controller.create({id:"r",requestId:"q",goal:"g",risk:"low",status:"CREATED",budgetLimit:5,spent:0,checkpoint:{}});projects.linkRun("t","r",{id:"owner"});state.transition("r",["CREATED"],"RUNNING",{});await interventions.execute("r","pause",{requestId:"i",actorId:"owner",reason:"operator inspection",expectedStatus:"RUNNING"});const projection=new OfficeProjectionStore(state.db,operations).catchUp("c");assert.equal(projection.phase,"idle");assert.ok(projection.timeline.some(x=>x.type.includes("intervention")));assert.deepEqual(new OfficeProjectionStore(state.db,operations).consistency("c").mismatches,[]);state.close();});
